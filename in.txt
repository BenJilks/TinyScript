
function Fib(int i) < int {
	if i > 3 
		return Fib(i - 1) + Fib(i - 2);
	return 1;
}

declare int X, /* STACK_SIZE + FRAME_SIZE */ 
	    Y; /* STACK_SIZE + FRAME_SIZE + 4 */
function Main() < int {
	X = 30;
	Y = Fib(X);
}
