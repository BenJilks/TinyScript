within io import log

class Vec2
{
    x y
}

class Cat
{
    name : int
    colour : Cat
    pos : Vec2

    func Cat(msg)
        log("Message: ", msg)
}

func test(cat : Cat)
    log(cat)

func main()
{
    idk : Cat = Cat()
    
    idk.pos = Vec2()
    idk.pos.x = 21
    idk.name = "my name jeff"

    log(idk.pos.x)
    log(idk.name)
}
