within io import log

class Cat
{
    name : int
    colour : Cat

    func Cat(msg)
        log("Message: ", msg)
}

func test(cat : Cat)
    log(cat)

func main()
{
    idk : Cat = Cat()
    test(idk)
}
