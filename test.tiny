within io import log
within maths import fib

class Vec2
{
    x y
}

class Cat
{
    name : int
    colour : Cat
    pos : Vec2

    func Cat(msg)
        log("Message: ", msg)
}

func test(cat : Cat)
    log(cat)

func main()
{
#    idk = Cat()
#    log(idk.Cat:pos)
    log(fib(5))
}
