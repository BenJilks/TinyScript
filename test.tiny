within io import log

class Vec2
{
    x y

    func Vec2(x, y)
    {
        self.x = x
        self.y = y
    }

    func Print()
        log(self.x, ", ", self.y)
}

func main()
{
    vec : Vec2 = Vec2(1, 2)
    vec.Print()
}
